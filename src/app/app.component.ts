import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  // direectly code can be written instead of path using template instead of templateUrl: ` `
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  title = 'conFusion';
}
